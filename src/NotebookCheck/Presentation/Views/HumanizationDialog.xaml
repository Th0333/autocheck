<Window x:Class="NotebookCheck.Presentation.Views.HumanizationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:NotebookCheck.Presentation.Controls"
        Title="Humanização"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="{DynamicResource Ink50Brush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="6"
                            CornerRadius="0" GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:ModalTitleBar Grid.Row="0" Title="Humanização"/>

        <StackPanel Grid.Row="1" Margin="22,18,22,20">
            <TextBlock Style="{StaticResource H2}" Text="⏱ Quantas horas de humanização?"/>
            <TextBlock Style="{StaticResource Subtle}" Margin="0,6,0,16" TextWrapping="Wrap"
                       Text="Cada ciclo dura ~1 hora (navegação, vídeo, picos de CPU, câmera, disco e idle). Escolha a duração total — dá pra parar antes a qualquer momento pelo botão ⏹."/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <Button x:Name="MinusBtn" Content="−" Click="OnMinus" ToolTip="1 hora a menos"
                        MinWidth="44" Width="44" Height="40" Padding="0" FontSize="18"/>
                <TextBox x:Name="HoursBox" Width="64" MinHeight="40" Margin="8,0" MaxLength="2"
                         FontSize="16" FontWeight="SemiBold"
                         Foreground="{DynamicResource Ink800Brush}"
                         PreviewTextInput="OnHoursInput"/>
                <Button x:Name="PlusBtn" Content="+" Click="OnPlus" ToolTip="1 hora a mais"
                        MinWidth="44" Width="44" Height="40" Padding="0" FontSize="18"/>
                <TextBlock Text="horas" Margin="12,0,0,0" VerticalAlignment="Center" FontSize="14"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <Button Content="6h" Style="{StaticResource BtnGhost}" MinWidth="52" Padding="10,6" Click="OnPreset" Tag="6"/>
                <Button Content="12h" Style="{StaticResource BtnGhost}" MinWidth="52" Padding="10,6" Click="OnPreset" Tag="12"/>
                <Button Content="24h" Style="{StaticResource BtnGhost}" MinWidth="52" Padding="10,6" Click="OnPreset" Tag="24"/>
            </StackPanel>

            <TextBlock x:Name="hintText" FontSize="12" Foreground="{DynamicResource Ink500Brush}"
                       HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"
                       Text="Entre 1 e 99 horas (padrão: 12)."/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                <Button Content="Cancelar" Style="{StaticResource BtnGhost}" IsCancel="True" Click="OnCancel"/>
                <Button Content="▶ Iniciar humanização" Style="{StaticResource BtnPrimary}" IsDefault="True" Click="OnStart"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
