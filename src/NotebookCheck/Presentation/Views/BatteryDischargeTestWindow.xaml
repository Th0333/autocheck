<views:TestResultWindow x:Class="NotebookCheck.Presentation.Views.BatteryDischargeTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:NotebookCheck.Presentation.Controls"
        xmlns:views="clr-namespace:NotebookCheck.Presentation.Views"
        Title="Descarga de bateria"
        Width="580" Height="540"
        MinWidth="520" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="{StaticResource Ink50Brush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="6"
                            CornerRadius="0" GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <controls:ModalTitleBar Grid.Row="0" Title="Descarga de bateria"/>

        <DockPanel Grid.Row="1" LastChildFill="True" Margin="20">
            <StackPanel DockPanel.Dock="Top">
                <TextBlock Style="{StaticResource H2}" Text="🔋 Teste de descarga da bateria"/>
                <TextBlock Style="{StaticResource Subtle}" Margin="0,4,0,12" TextWrapping="Wrap"
                           Text="Confirma que o notebook se mantém ligado na bateria e mede a taxa de descarga real. Desconecte o carregador quando solicitado e observe a leitura ao vivo."/>
            </StackPanel>

            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="✗ Falha" Style="{StaticResource BtnGhost}" Click="OnFailed"/>
                <Button x:Name="okBtn" Content="✓ Funciona (OK)" Style="{StaticResource BtnSuccess}" Click="OnOk"/>
            </StackPanel>

            <StackPanel>
                <!-- Banner de estado AC -->
                <Border x:Name="acBanner" Padding="12,10" CornerRadius="8" Margin="0,0,0,12"
                        Background="{StaticResource Ink100Brush}">
                    <TextBlock x:Name="acText" Text="Lendo bateria..." FontWeight="SemiBold"
                               Foreground="{StaticResource Ink800Brush}"/>
                </Border>

                <Border Style="{StaticResource Card}" Padding="18">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource Label}" Text="CARGA ATUAL"/>
                                <TextBlock x:Name="chargeText" Text="—" FontSize="30" FontWeight="Bold"
                                           Foreground="{StaticResource BrandBrush}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Style="{StaticResource Label}" Text="DESCARGA"/>
                                <TextBlock x:Name="rateText" Text="—" FontSize="30" FontWeight="Bold"
                                           Foreground="{StaticResource BrandBrush}"/>
                            </StackPanel>
                        </Grid>

                        <Separator Margin="0,12" Background="{StaticResource Ink100Brush}"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource Label}" Text="AUTONOMIA ESTIMADA"/>
                                <TextBlock x:Name="runtimeText" Text="—" FontWeight="SemiBold" FontSize="16"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Style="{StaticResource Label}" Text="TENSÃO"/>
                                <TextBlock x:Name="voltText" Text="—" FontWeight="SemiBold" FontSize="16"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Margin="0,12,0,0" Padding="14" CornerRadius="8"
                        Background="{StaticResource Ink50Brush}" BorderBrush="{StaticResource Ink200Brush}"
                        BorderThickness="1">
                    <TextBlock x:Name="statusText" TextWrapping="Wrap" Foreground="{StaticResource Ink700Brush}"
                               Text="Conecte o carregador e depois DESCONECTE-O para medir a descarga real."/>
                </Border>

                <!-- Sem bateria -->
                <Border x:Name="noBatteryPanel" Style="{StaticResource Card}" Padding="18"
                        Margin="0,12,0,0" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="⚠ Nenhuma bateria detectada" FontWeight="SemiBold" FontSize="15"
                                   Foreground="{StaticResource WarnBrush}"/>
                        <TextBlock Style="{StaticResource Subtle}" Margin="0,8,0,0" TextWrapping="Wrap"
                                   Text="Não foi possível enumerar uma bateria neste equipamento (desktop ou bateria ausente/não reconhecida). Marque o resultado manualmente."/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </DockPanel>
    </Grid>
</views:TestResultWindow>
