<UserControl x:Class="NotebookCheck.Presentation.Controls.ModalTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             x:Name="root">
    <Border Background="{StaticResource DarkPanelBrush}" Height="36"
            shell:WindowChrome.IsHitTestVisibleInChrome="False">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                <Image Source="/Assets/notelet-logo.png" Height="20" Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Title, ElementName=root}" Margin="10,0,0,0" Foreground="White"
                           FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Click="OnMinimize" Style="{StaticResource WinCtrlBtn}" ToolTip="Minimizar">
                    <Path Data="M 0,4 H 10" Stroke="White" StrokeThickness="1"/>
                </Button>
                <Button Click="OnMaximize" Style="{StaticResource WinCtrlBtn}" ToolTip="Maximizar">
                    <Path Data="M 0,0 H 10 V 10 H 0 Z" Stroke="White" StrokeThickness="1" Fill="Transparent"/>
                </Button>
                <Button Click="OnClose" Style="{StaticResource WinCtrlClose}" ToolTip="Fechar">
                    <Path Data="M 0,0 L 10,10 M 10,0 L 0,10" Stroke="White" StrokeThickness="1.2"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
